# MomentRead 开发与验收清单

日期：2026-09-11。生产工程独立于保留的 `prototype/`。当前已完成阅读与学习主链，**自动原著发现和最终提交盲测尚未放行**。

| 阶段 | 所有者 | 实现情况 | 放行证据／待办 |
|---|---|---|---|
| P0 公共契约与脚手架 | 主 Agent | 已实现 | Zod 类型／校验、ports、HTTP、生成 OpenAPI、Node 22 与依赖锁 |
| P1 存储／书库 | A | 已完成 | SQLite、复制导入、受控文件、真实备份恢复；译者／版本资料与旧工作区兼容已回归 |
| P1 EPUB | B | Chrome 已实测 | 真实《虚无主义》、目录、跨段选择、CFI；字号跨章恢复有合成浏览器证据；IAB 兼容未通过 |
| P1 Claude Code | C | 已完成对话能力 | 首次、明确接续、兄弟隔离、取消、权限、结构化结果和固定 URL 取回通过；WebSearch 空结果单列为匹配阻塞 |
| P2 学习与概念 | D | 已实现 | 背景截点、逐层回馈、修订、义项隔离、幂等、500 节点／20 层测试 |
| P2 原著匹配 | E | 取回核验已实测，发现受阻 | 德／英／拉丁公开正文独立取回、逐字核对；Claude 搜索 403，Bing RSS 结果不可信，未启用替代 |
| P2 界面接入与成果 | F | 已完成 | 真实服务、分支草稿、小结、档案、报告、备份；完整与流式 Markdown、选区映射、错误与重试状态已测 |
| P3 集成与恢复 | 主 Agent | 已完成 | 真实书籍／真实 CLI 闭环、逐层回馈、真实重启、断网 HTML、空目录恢复通过 |
| P4 独立接口／系统测试 | G | 已完成 | 240 项 Vitest；最终构建 21 项布局规模 + 4 项来源 UI 全通过 |
| P4 全新上下文盲测 | H | 部分完成 | 第一轮基础、重启、离线与恢复通过，AI 分支被当时 429 阻断；最终提交需换新测试者复跑 |
| P5 最终交付 | 主 Agent | 未放行 | A20 最终盲测与有效 WebSearch 候选仍缺；详见 [验收记录](ACCEPTANCE.md) |

各 Agent 的变更范围、输入输出与测试见 [接口契约](CONTRACTS.md)、[HTTP](HTTP.md) 和模块说明。公共依赖、接口变更、集成与 Git 由主 Agent 串行管理；子 Agent 不操作暂存区，不推送。

实际书籍测试只使用用户指定文件的本地副本，原文件保持不变；私人 EPUB、选段、CLI 输入输出和含正文截图保存在忽略的 `tmp/` 中，不进入 Git。可提交截图只使用合成材料。

## 放行判定

- [PRD A01–A21](../PRD.md) 原要求保留，不因未实现或验证失败而修改成通过。
- 来源取回成功不等于搜索成功，模型运行完成不等于工具成功。
- HTML 要在服务关闭、该测试浏览器断网后打开；ZIP 要在空数据目录恢复。
- README 命令必须在独立目录真实执行；盲测不读取源码或开发操作脚本。
- 独立测试发现的缺陷必须由开发者修复，再以原断言复验，详见 [SYSTEM-TESTS.md](SYSTEM-TESTS.md)。
