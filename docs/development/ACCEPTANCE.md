# MomentRead 首版验收记录

验收日期：2026-09-11。测试书为用户授权的《虚无主义》EPUB；原文件 SHA-256 为 `42ce431b388d02b57ce35c0a638b6c6447c9030d7328b6d01a60a4237c34c86d`，导入前后保持一致。私人正文、模型输出和真实截图只保存在忽略的 `tmp/`，不进入 Git。

## 总体判定

阅读、讨论树、Markdown、逐层回馈、重启接续、今日总结、离线 HTML 和备份恢复已实际贯通。自动原著发现仍未放行：Claude Code WebSearch 能完成权限往返，但本次返回零条结果；系统会保留“尚未核对”，不会把空结果或模型回译当原文。

固定自动化基线：21 个 Vitest 文件、242 项通过；权限交互改动后的最终前端构建有 21 项三尺寸／500 节点／20 层浏览器检查及 4 项来源状态检查通过。真实 Claude Code 首次调用、明确会话接续、结构化小结、今日总结和固定 URL WebFetch 通过。CLI 请求别名为 `claude-opus-4-8[1m]`，当前 provider 实际响应模型字段为 `glm-5.3-flash`，两者不混称。

## A01–A21

| 条目 | 状态 | 证据与边界 |
|---|---|---|
| A01 | 通过 | 真实 EPUB 在 Chrome 导入、目录、字号、跨段选择、CFI 定位和重开恢复；Codex 内嵌浏览器兼容未承诺。 |
| A02 | 通过 | 真实 26 字选段建立根讨论，绑定 bookId、fileVersionId、spine 和 CFI；真实 AI 返回。 |
| A03 | 通过 | 在真实回答中选取“或然性”建立分支，正文位置不变，活动蓝圈切换。 |
| A04 | 通过 | 三尺寸与 500 节点浏览器检查：默认无文字、焦点／悬停可识别、仅活动节点蓝圈。 |
| A05 | 通过 | 真实兄弟草稿和合成浏览器兄弟往返均独立保存。 |
| A06 | 通过 | 实际界面编辑“或然性”小结后确认，只返回根段落；提示与 receipt 一致。 |
| A07 | 通过 | 失败、过期预览、幂等重试和事务回滚测试通过；今日失败会显示原因并可重试。 |
| A08 | 部分通过 | 失败／歧义／未取回状态和继续中文解释通过；自动 WebSearch 本次零结果，未通过有效候选发现。 |
| A09 | 通过 | 同名义项按书籍、语境和来源分别保存，背景提示不强套单一定义。 |
| A10 | 通过 | 新原著文件版本、受影响记录和历史来源版本测试通过；真实德／英／拉丁公开文本独立取回核对通过。 |
| A11 | 通过 | 实际停止服务并重启，书籍、位置、活动讨论、草稿、小结和失败记录读回。 |
| A12 | 通过 | 含真实确认小结与 AI 建议的 HTML 在服务停止、独立浏览器断网时打开，零外部资源。 |
| A13 | 通过 | 实际跳过回顾后继续提问，回答结合已确认“归纳”且不重复设门槛。 |
| A14 | 通过 | 同一根选段包含“或然性”“因果性”两个兄弟，来源相同、会话和草稿隔离。 |
| A15 | 通过 | 真实树为根→或然性→归纳→经验；父级、rootId、背景和路径正确。 |
| A16 | 通过 | “经验”回馈归纳、“归纳”回馈或然性、“或然性”回馈根段落，均只到直接父级。 |
| A17 | 通过 | 兄弟和深层活动节点重启恢复测试通过；其他节点未自动完成。 |
| A18 | 通过 | 子小结修订后父级 needsMerge、旧 receipt 保留及再次整理测试通过。 |
| A19 | 通过 | 500 节点、20 层，三尺寸折叠 500→44、展开回 500，活动路径和数据保持。 |
| A20 | 待最终复测 | 第一轮无历史盲测的安装、导入、重启、离线 HTML 和恢复通过；额度恢复后的第二轮完成导入、阅读、真实解析与 Markdown，发现同轮 49 次权限提示。该缺陷已修复并由开发者真实界面复验；仍需新的无历史测试者复跑修复后的完整链。 |
| A21 | 通过 | 429 期间阅读、草稿、位置和记录可访问；安全错误分类、重试次数和可行操作提示已补测。 |

## 关键实测

- 真实学习树：根段落、两个兄弟、三层概念分支；真实讨论输出分别绑定明确 discussionId、runId 和 CLI session。
- 额度恢复后：首次调用成功；同一“或然性”会话连续两次明确接续；结构化小结生成、编辑确认、直接父级返回成功。
- 今日总结：按 `date + timezone` 保存目标，真实生成 483 字 Markdown；页面呈现标题、列表和加粗。
- 权限交互：盲测复现同轮 49 次提示；修复后真实产品只提示一次，一次允许结清 2 个并发请求，20 秒内未重现。
- 恢复：最终 ZIP 导入空数据目录后为 1 本书、5 个讨论、3 个确认小结、3 条逐层 receipt；CFI、活动节点和书籍文件指纹一致，0 个遗留运行。
- 规模与布局：[最终浏览器记录](BROWSER-SCALE.md)。第一轮盲测的独立证据保存在 `/tmp/momentread-first-use-h.WwVKWN/REPORT-H.md`，不随仓库提交。

## 尚未放行的项目

1. 自动原著发现必须返回至少一条可独立取回的候选才能记为成功；当前 WebSearch 为空，固定 URL WebFetch 成功不能替代发现。
2. A20 需要在这一最终提交上由新的无历史测试者完成真实 AI 分支与逐层返回；开发者实测不能冒充盲测。
