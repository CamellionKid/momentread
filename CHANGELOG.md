# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.1] - 2026-09-12

### 修复

- **opencode 工作目录固定**：子进程统一使用规范化的绝对运行目录，显式设置 `PWD` 环境变量并传入 `--dir`，消除 OpenCode 优先读取父进程 `PWD` 导致未加载专用 `momentread` agent 的失败（此前表现为约 1 秒 `UnknownError` 且无正文）；`run`、`probe` 与模型枚举使用同一二进制与同一环境构造方式。
- **opencode 错误分类与诊断**：兼容 `error.message` 与 `error.data.message` 两种错误结构，提取错误名称、状态码与引用编号，区分认证、限流、模型不存在、配置/agent 错误、超时与未知错误；界面显示脱敏原因与下一步；成功判定以进程退出码与信号为准，不再仅凭收到 `step_finish`。
- **会话按运行时隔离**：讨论会话记录所属运行时，切换运行时后从已保存讨论背景创建新会话，不再跨运行时复用（修复「会话标识无效，不能接续」）；历史缺少归属的会话按不可复用处理；取消、权限答复与关闭均路由到运行各自的 adapter。
- **运行时切换原子性**：先验证目标运行时与模型，再在与任务启动共享的临界区内一次性提交配置；模型验证失败时保持原配置，不再出现 provider 已改而 model 校验失败的中间态。

### 工程

- 新增覆盖运行目录固定（含带空格路径）、错误协议分类、会话运行时归属与切换并发的测试；真实 CLI 验收覆盖首次回答、明确会话接续、JSON 小结与取消后再提问。

## [0.3.0] - 2026-09-12

### 新增

- **设置界面切换 AI 运行时**：「AI 连接与本地数据」对话框新增运行时切换（Claude Code / opencode），不重启即时生效并持久保存；有正在进行的生成时拒绝切换并提示，切换前自动检测目标运行时的安装状态。
- **opencode 作为可选 AI 运行时**：opencode 驱动讨论、概念展开、小结与每日总结；原著检索在 opencode 下暂不支持并会明确提示。默认仍为 Claude Code。
- **模型选择**：「AI 连接与本地数据」对话框按当前运行时提供模型下拉（opencode 取自 `opencode models`），两个运行时的模型选择独立持久保存；`MOMENTREAD_AI_MODEL` 可指定默认模型。

### 调整

- `MOMENTREAD_AI` 环境变量降级为首次启动的默认运行时；在界面切换过运行时后，以界面选择为准。

## [0.2.1] - 2026-09-11

### 调整

- **提问消息改为圆角卡片**：讨论面板中的用户提问从透明底左侧竖线改为圆角卡片排版，「你」标签收进卡片内，与 AI 回答明确分层；默认中性灰色号，样式表保留品牌蓝、暖灰米两套备选主题。

## [0.2.0] - 2026-09-11

### 新增

- **已解析选段波浪线**：正文中经过「解析一下」的选段持久显示灰白色波浪下划线，基于 CSS Custom Highlight API 实现，不改动 DOM、不影响选段定位（CFI）。
- **讨论面板浮动操作条**：在 AI 回答里选中文字后立即浮出「展开已选中的概念」操作条，无需再滚动到每条回答底部点击按钮；支持一键取消选中，无法定位的选区给出错误提示。
- **翻页 / 滚动双模式**：阅读设置中可切换「上下滚动」与「左右翻页」，翻页模式下支持工具条按钮与 PageUp/PageDown 翻页；选择按书持久化，切换后保持当前阅读位置。

### 工程

- 新增 GitHub Actions CI（typecheck、单元 / 集成 / 契约测试、构建）与 tag 触发的 Release 工作流。

## [0.1.0] - 2026-09-10

- 首版基线：EPUB 阅读、选段 AI 解析、概念分支讨论、整理返回阅读主线。
