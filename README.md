# MomentRead

在浏览器里阅读 EPUB，按需请求 AI 解析，在概念分支中深入讨论，再整理并返回原来的阅读思路。

**当前状态：需求与静态视觉设计阶段，尚无可执行应用。** 本文是安装和使用说明草稿；依赖版本、配置路径、安装与启动命令将在选型和实现验证后补齐。不要把本文当成已经可以运行的快速开始指南。

需求与验收见 [PRD](docs/PRD.md)，已确认的界面见 [设计说明](docs/design/README.md)。

## 项目资料

本仓库是 MomentRead 后续开发与文档维护的主目录。2026-09-10 从前期需求讨论中导入资料；旧目录的文件保留为历史副本。

- [PRD](docs/PRD.md)：功能、验收场景、已确认决定与待决技术选型。
- [初始需求](docs/original-requirements.md)：用户最初的需求文本快照，保留原样。
- [选定原型](docs/design/selected-prototype.png)：已认可的深色、多级概念分支修订图。
- [线路参考图](docs/design/route-reference.png)：用户提供的最初线路结构参考。

## Git 管理

- 本地独立仓库，主分支为 `main`；提交使用 `[scope] short imperative description` 格式。
- 每次只提交本次相关文件，提交前检查差异；不将其他项目或旧 Vault 仓库一起提交。
- `.gitignore` 排除凭据、依赖、构建缓存、日志和本地书籍／学习数据；`.env.example` 可用于无真实凭据的配置示例。
- 当前没有配置远程仓库。远程地址、仓库可见性和推送待另行确定。
- 初始化提交只包含设计与说明资料，不表示应用功能已经实现。

## 使用方式

首版采用本地网页版：用户自行安装依赖、配置 AI 环境，启动本地服务后，通过桌面浏览器使用。不需要公网部署，也不以“别人打开网址即可使用”为首版目标。

界面包括左侧 EPUB、右侧当前 AI 讨论、最右侧概念线路图。一个选段可以展开多个概念讨论，每个讨论又可以继续展开子分支。用户通过“整理并返回”逐层回到直接父讨论。

## 安装前需要准备什么

| 项目 | 当前要求／状态 |
|---|---|
| 电脑与浏览器 | 以 Darren 的 macOS 桌面使用场景为起点；其他平台兼容性待验证 |
| 书籍 | 可选中文字的 EPUB；可以另行补充外文原著 |
| 应用运行依赖 | 具体运行时、版本和包管理器待选型后固定 |
| AI 运行环境 | 候选为 OpenCode、Claude Code、DeepSeek Harness；尚未确定首个后端，不需要现在全部安装 |
| 模型访问方式 | 按最终后端支持的认证和计费方式配置；已有聊天订阅不自动等于程序化调用可用 |
| 网络 | 使用远程模型和原著检索时需要；本地网页不等于所有 AI 功能离线运行 |

## 快速开始的交付要求

正式 README 将提供下列可复制、已验证的步骤；目前不填入虚构仓库地址、端口或不存在的脚本。

1. 获取项目：实际发布仓库、支持版本，以及需要下载的文件。
2. 安装依赖：准确的运行时版本与安装命令。
3. 配置 AI：支持的后端、认证说明、模型选择及检索配置。
4. 启动：产品和 AI 服务各自的命令，或一个已经实现的统一启动入口。
5. 打开网页：启动输出给出的本地地址，以及正常连接的可观察状态。
6. 导入 EPUB：完成一次选段解析，展开两个同级概念，再从其中一个展开子分支。
7. 整理并返回：确认小结回到直接父讨论，再恢复段落解析。
8. 关闭与接续：正常停止服务，重启后恢复书籍位置及活动讨论。

首次模型连接检查应使用无私人内容的简单请求；确认能够返回后，再处理选定的书籍内容。

## 数据保存与备份

建议 EPUB 引用、阅读位置、讨论关系、确认小结和概念档案保存在本机。最终数据目录、文件导入方式、数据库和备份操作尚未确定。

正式 README 必须说明：

- EPUB 是复制导入还是引用原文件，移动原文件会有什么影响。
- 学习数据与 AI 后端会话分别保存在哪里，如何一起备份和恢复。
- 哪些选段或背景会发给远程模型／检索服务，凭据配置在哪里。
- 更新应用时如何保留数据；需要迁移时如何恢复旧版本。
- HTML 总结是给人回看的阅读产物，不能作为完整数据备份。

## 常见问题应如何排查

| 现象 | 正式说明必须提供的排查路径 |
|---|---|
| 网页打不开 | 产品服务是否启动、实际地址、端口冲突处理 |
| 可以读书但 AI 不回复 | AI 服务连接、认证、所选模型及请求失败信息 |
| 找不到外文原著 | 展示检索状态，允许继续中文解析或补充文件，不伪造匹配结果 |
| 重启后无法恢复讨论 | 产品数据目录和后端会话目录是否一致，定位失效原因并保留已有记录 |
| 深层分支不好找 | 路径导航、折叠展开及返回直接父讨论的方法 |

## 当前参考资料

- [OpenCode Server](https://opencode.ai/docs/server/)
- [Claude Code 程序化调用](https://code.claude.com/docs/en/headless)
- [DeepSeek Harness](https://github.com/deepseek-ai/deepseek-harness)

以上链接供选型参考，不表示本产品已经完成集成。正式发布前，需在干净环境中只依照 README 完成安装、首次阅读、嵌套分支返回和重启恢复，记录验证结果。
